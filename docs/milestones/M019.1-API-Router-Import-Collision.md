# M019.1 – API Router Import Collision

The project already had a `src.api.routes` package. Python packages take
the import path used by `from src.api.routes import router`, so the new
`routes.py` module was not imported.

The closed FastAPI router now lives in `src.api.http_routes`, avoiding
the existing package namespace while preserving all M019 endpoints and
request contracts.
