# F100.5.2 — NewRules 5-2-23 Classification Correction

## Source

Confirmed via live SQL evidence, 2026-08-20: `cfg.FieldOptionDependency`
rows in the current ACTIVE metadata publication (`docs/evidence/F120/
FYBROC_SQL_METADATA_SNAPSHOT.json`) include `SourceWorksheet =
'NewRules 5-2-23'`, `SourceWorkbook = 'Price Estimator-Fybroc.xlsm'`.
This is a different evidence tier than F100.5.1 (which was based on a
stated fact) - this one is based on direct inspection of what the live,
currently-published database actually compiled from.

## What F100.5 got wrong

`NewRules 5-2-23` (`Price Estimator-Fybroc.xlsm`) was classified
`NEEDS_ENGINEERING_REVIEW` - no established mapping or keyword match.
It was flagged in the F100 engineering-questions document as unclear:
*"the date in the name suggests a rule change introduced on a specific
day. Still active, or superseded since?"*

## Correction

Not superseded. It is an active source for dependency rules in the
metadata publication the running application currently uses. The
question wasn't wrong to ask - the answer just turned out to be sitting
in the database rather than needing an engineering conversation.

## Reclassification

`NewRules 5-2-23` should carry the tag: `dependencies`

Confidence: **ESTABLISHED**, citing this document and the underlying
SQL evidence (`cfg.FieldOptionDependency.SourceWorksheet`), not a
keyword match or a stated fact.